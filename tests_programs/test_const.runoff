# SUper nice comment :angrysad:
const abc 123;
const loses 321; #comment test

function setup() -> void
{
  int8 a = 321;
  int16 b = 123;
  int32 c = 32123;
  int32 c = 123 + 321;
}

function dummy(int a) ->  {
  print(a);
  bool state = true;
  while(true) {
    int ledPin = 5;
    setoutput(ledPin, state);
    state = !state;
    delay(1000);
  }
}

task a(bool is_so, int8 pinInput) {
  for(int i = 0; i <= 5; i++) {
    check_pinInput(pinInput);
    is_so = !is_so;
  }

}
