
function setup() -> void {
  int8 a = 321;
  int16 b = 123;
  int32 c = 32123;
  int32 c = 123 + 321;
  return;
}

function ss(int a) -> struct hello {
    struct hello test;
    return 1+2+3*(3+7) + 12;
    return test;
}

function dummy(int a) -> int {
  print(a);
  bool state = true;
  while(true) {
    int ledPin = 5;
    setoutput(ledPin, state);
    state = !state;
    delay(1000);
  }

  return 1;

  switch(a) {
    case 1:
    case 2:
    case 3:
    case 4:
        print(a);
    default:
        print(karl_er_en_so);
  }

  receive {
    case Ping: print(1);
    case Pong(a, b, c): print(a);
  }



  if (so123) {
    print(abc);
    int a = 123;
    int64 c = abc;
  }else {
    print(nu_er_peter_en_so);
  }

  if (true) {
    print(sunglasses);
  }

  if (false) {

  }elseif (true) {
    print(angrysad);
  }else {
    print(oh_why);
  }

  (true) ? print(hej) : print(last);
  1+2 ? print(hej) : print(last);
  1*2+3/123 ? print(hej) : print(last); 
}
