pinid AnalogInPin = 123P;
pinid AnalogOutPin = 234P;

function test() -> void {
        readAnalogInput(AnalogInPin);
        uint8 outval = 100;
        analogWrite(AnalogOutPin, outval);
}

function setup() -> void {
	test();
}

