pinid AnalogInPin = 123;
pinid AnalogOutPin = 234;

function test() -> void {
        int val =  readAnalogInput(AnalogInPin);
        int outval = 100;
        analogWrite(AnalogOutPin, outval);
}

function setup() -> void {
        analogInputPin(AnalogInPin);
        analogOutputPin(AnalogOutPin);
}

